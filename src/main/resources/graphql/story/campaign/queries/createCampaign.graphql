mutation CreateCampaign($input: CreateCampaignInput!) {
    createCampaign(input: $input) {
        campaign {
            id
            name
            description
            imageUrl
            auditingMetadata {
                createdBy
                lastModifiedBy
                creationDate
                lastModifiedDate
            }
        }
    }
}
