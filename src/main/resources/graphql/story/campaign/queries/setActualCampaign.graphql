mutation SetActualCampaign($input: SetActualCampaignInput!) {
    setActualCampaign(input: $input) {
        campaign {
            id
            name
            description
            imageUrl
            actual
            auditingMetadata {
                createdBy {
                    name
                }
                lastModifiedBy {
                    name
                }
                creationDate
                lastModifiedDate
            }
            version
        }
    }
}
