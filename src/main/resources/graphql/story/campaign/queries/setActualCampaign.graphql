mutation SetActualCampaign($input: SetActualCampaignInput!) {
    setActualCampaign(input: $input) {
        campaign {
            id
            name
            description
            imageUrl
            actual
            auditingMetadata {
                createdBy {
                    id
                    userName
                }
                lastModifiedBy {
                    id
                    userName
                }
                creationDate
                lastModifiedDate
            }
        }
    }
}
