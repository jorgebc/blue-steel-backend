mutation UpdateCampaign($input: UpdateCampaignInput!) {
    updateCampaign(input: $input) {
        campaign {
            id
            name
            description
            imageUrl
            auditingMetadata {
                createdBy
                lastModifiedBy
                creationDate
                lastModifiedDate
            }
        }
    }
}
